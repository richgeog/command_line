The cp command in shell is the copy command.

This is used for copying files to other direcrtories or withij the same directory. When you copy the name can also be changed.

An example of what cp would look like in shell is:

$ cp a_file another_file
